package by.markov.strings.arrayofstring.second;
// Замените в строке все вхождения 'word' на 'letter'.

public class ChangeWord {
    public String changeWord(String line) {
        System.out.println(line.replace("word", "letter"));
        return line;
    }
}
