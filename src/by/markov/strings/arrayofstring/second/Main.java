package by.markov.strings.arrayofstring.second;

public class Main {
    public static void main(String[] args) {
        String line = "Hello word. I changed a word.";
        ChangeWord changeWord = new ChangeWord();
        changeWord.changeWord(line);
    }
}
